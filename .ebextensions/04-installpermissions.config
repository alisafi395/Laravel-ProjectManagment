container_commands:
  01_fix_hook_perms:
    command: |
      if [ -d ".platform/hooks/predeploy" ]; then
        chmod +x .platform/hooks/predeploy/* || true
      fi
      if [ -d ".platform/hooks/postdeploy" ]; then
        chmod +x .platform/hooks/postdeploy/* || true
      fi
